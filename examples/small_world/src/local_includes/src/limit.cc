#include <local_includes/limit.hpp>

// Generated into the package's genfiles directory, so this only resolves
// through local_includes = ["$(GENDIR)"].
#include "generated_limit.hpp"

namespace small_world {

int limit() { return kGeneratedLimit; }

}  // namespace small_world
